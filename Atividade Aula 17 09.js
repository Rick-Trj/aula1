const readline = requere ('readline')

const r1 = readline.createInterface({
    imput:process.stdin,
    output:process.stdout
});

r1.question('Se o n número for PAR:', (par) =>{
    
    console.log(`O número correspondente é:  ${par}`);

    if (par) {
        par
     } else {
        número12
     }

    r1.question('Se o n número for IMPAR :', (impar) =>{
    
         console.log(`O número correspondente é:  ${impar}`);
        
         if (impar) {
            impar
         } else {
            número12
         }
        
            r1.question('Se o n número for zero:', (zero) =>{
    
                console.log(`O número correspondente é:  ${zero}`);
        
                if (impar) {
                 impar
                } else {
                número12
        
             } 

});
    });

        });